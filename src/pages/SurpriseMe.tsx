import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export function SurpriseMe() {
    return (
        <SafeAreaView>
            <Text> Find My Game </Text>
        </SafeAreaView>
    )
}